export type OrderItem = {
  id: number;
  orderId: number;
  productId: string;
  sellerId: string;
  price: string;
  commissionRate: string;
  commissionAmount: string;
  sellerAmount: string;
  createdAt: string;
};
